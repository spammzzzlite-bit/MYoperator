# Secrets — never commit
.env
.env.*
!.env.example

# Local data (raw Airtable snapshot + raw record samples). Contains candidate
# personal data and the repo is public, so none of it is committed.
# Regenerate with the pipeline in README.md.
data/

# Python
__pycache__/
*.pyc
.venv/
venv/
